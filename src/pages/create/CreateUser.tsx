import { UserForm } from "../../components/form/UserForm";


export const CreateUser = (): JSX.Element => (
  <section className="section-top">
    <h1 className="header-title">Create a new user</h1>
    <UserForm />
  </section>
)